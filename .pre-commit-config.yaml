repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.3.7
    hooks:
      - id: ruff
        args: [--fix]
        exclude: ^src/offers_sdk/generated/
      - id: ruff-format
        exclude: ^src/offers_sdk/generated/

  - repo: https://github.com/pycqa/isort
    rev: 5.13.2
    hooks:
      - id: isort
        args: [--profile=black]
        exclude: ^src/offers_sdk/generated/

  - repo: https://github.com/psf/black
    rev: 24.10.0
    hooks:
      - id: black
        language_version: python3
        exclude: ^src/offers_sdk/generated/

  # Note: mypy is excluded from pre-commit due to generated files issues
  # Run mypy manually: poetry run mypy --config-file=mypy.ini src/

  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: poetry run pytest
        language: system
        pass_filenames: false
        always_run: true
        stages: [manual]
